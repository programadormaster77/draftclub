name: Build iOS IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      # =========================================================
      # 📦 CHECKOUT DEL REPOSITORIO
      # =========================================================
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      # =========================================================
      # 🧰 CONFIGURAR FLUTTER (usa la MISMA versión que tu entorno)
      # =========================================================
      - name: 🧰 Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.35.4"
          channel: stable
          cache: true

      - name: 🔍 Verify Flutter version
        run: flutter --version

      # =========================================================
      # 📦 DEPENDENCIAS DE FLUTTER
      # =========================================================
      - name: 📦 Get Flutter packages
        working-directory: apps/mobile
        run: flutter pub get

      # =========================================================
      # ⚙️ PREPARAR ENTORNO iOS (descarga engine y genera podhelper.rb)
      # =========================================================
      - name: 🛠️ Precache iOS artifacts
        run: flutter --no-version-check precache --ios

      # Genera ios/Flutter/podhelper.rb SIN exigir firmas
      - name: ⚙️ Generate iOS structure (simulator-safe / no codesign)
        working-directory: apps/mobile
        env:
          CI: true
          CODE_SIGNING_ALLOWED: "NO"
          CODE_SIGN_IDENTITY: ""
          EXPANDED_CODE_SIGN_IDENTITY: ""
          FLUTTER_SUPPRESS_ANALYTICS: "true"
        run: |
          # Genera la carpeta ios/Flutter y el podhelper.rb
          flutter build ios --simulator --no-codesign || true
          test -f ios/Flutter/podhelper.rb && echo "✅ podhelper.rb OK" || (echo "❌ Falta podhelper.rb" && exit 1)

      # =========================================================
      # 🧹 COCOAPODS - instalación estable via Homebrew
      # =========================================================
      - name: 🧹 Install CocoaPods via Homebrew
        working-directory: apps/mobile/ios
        run: |
          echo "🧩 Installing CocoaPods with Homebrew..."
          brew update
          brew install cocoapods
          pod --version
          echo "🧹 Cleaning old pods..."
          rm -rf Pods Podfile.lock
          pod repo update
          pod install --repo-update --verbose

      # =========================================================
      # 🚀 COMPILAR Y EMPAQUETAR IPA (sin firmar)
      # =========================================================
      - name: 🚀 Build iOS app (Release, no codesign)
        working-directory: apps/mobile
        env:
          CI: true
          CODE_SIGNING_ALLOWED: "NO"
          CODE_SIGN_IDENTITY: ""
          EXPANDED_CODE_SIGN_IDENTITY: ""
        run: flutter build ipa --release --no-codesign

      # =========================================================
      # 📦 EXPORTAR IPA COMO ARTEFACTO DESCARGABLE
      # =========================================================
      - name: 📦 Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: draftclub-ios-ipa
          path: apps/mobile/build/ios/ipa/*.ipa

      # =========================================================
      # ✅ FINALIZACIÓN
      # =========================================================
      - name: ✅ Complete job
        run: echo "✅ Build iOS finished successfully!"
