name: Build iOS IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: 🧾 Set up job
        run: echo "Initializing iOS build job..."

      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: 🧰 Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.24.5"
          channel: stable
          cache: true

      - name: 🔍 Verify Flutter
        run: flutter --version

      - name: 📦 Get Flutter packages
        working-directory: apps/mobile
        run: |
          flutter pub get
          # Analiza el código, pero permite continuar si hay advertencias
          flutter analyze || echo "⚠️ Advertencias detectadas, pero el build continuará."

      - name: 🧩 Install CocoaPods
        working-directory: apps/mobile/ios
        run: |
          pod repo update
          pod install

      - name: 🛠️ Enable iOS build (no codesign)
        working-directory: apps/mobile
        run: flutter build ios --release --no-codesign

      - name: 🚀 Build iOS IPA (Release)
        working-directory: apps/mobile
        run: flutter build ipa --release --no-codesign

      - name: 📦 Package IPA
        uses: actions/upload-artifact@v4
        with:
          name: draftclub-ios-ipa
          path: apps/mobile/build/ios/ipa/*.ipa

      - name: ✅ Complete job
        run: echo "✅ Build process finished successfully!"
