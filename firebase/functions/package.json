{
  "name": "functions",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions",
    "clean": "rimraf lib"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^4.6.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "rimraf": "^5.0.5"
  }
}
