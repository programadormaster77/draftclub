name: draftclub_mobile
description: Aplicación oficial de DraftClub — Tu carrera, tu club.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

  visibility_detector: ^0.4.0

  # 🎧 Audio
  audioplayers: ^5.2.1
 

  # ============================================================
  # 🌐 Estado y navegación
  # ============================================================
  flutter_riverpod: ^2.5.1
  go_router: 17.0.0
  provider: ^6.1.2

  # ============================================================
  # 🌍 Networking y APIs
  # ============================================================
  http: ^1.2.2
  dio: ^5.7.0

  # ============================================================
  # 🔥 Firebase Suite — versiones compatibles
  # ============================================================
  firebase_core: ^3.4.0
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.3.0
  firebase_storage: ^12.3.0
  firebase_messaging: ^15.1.3
  firebase_crashlytics: ^4.1.3
  firebase_analytics: ^11.3.2
  firebase_app_check: ^0.3.2+1
  cloud_functions: ^5.1.3

  # ============================================================
  # 🟦 Autenticación Social
  # ============================================================
  flutter_facebook_auth: ^7.0.1
  google_sign_in: ^6.2.1

  # ============================================================
  # 🎨 UI / Multimedia / Animaciones
  # ============================================================
  cached_network_image: ^3.4.1
  video_player: ^2.9.2
  chewie: ^1.7.5
  lottie: ^3.1.0
  flutter_svg: ^2.0.10+1
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2

  # ============================================================
  # 📸 Captura, grabación y archivos
  # ============================================================
  image_picker: ^1.1.2
  record: ^6.1.2
  path_provider: ^2.1.4
  video_thumbnail: ^0.5.3
  path: ^1.9.0

  # ============================================================
  # 🗺️ Geolocalización
  # ============================================================
  geolocator: ^13.0.2
  geocoding: ^4.0.0
  intl: ^0.20.2
  uuid: ^4.5.0
  share_plus: ^10.0.2

  # ============================================================
  # 💰 Compras dentro de la app
  # ============================================================
  in_app_purchase: ^3.1.11

  # ============================================================
  # 🔗 Enlaces profundos
  # ============================================================
  app_links: ^6.4.1

  # ============================================================
  # 🔔 Notificaciones
  # ============================================================
  flutter_local_notifications: ^17.1.2
  permission_handler: ^11.3.1

  # ============================================================
  # ⚙️ Utilidades
  # ============================================================
  plugin_platform_interface: ^2.1.4
  flutter_launcher_icons: ^0.14.4

  # ============================================================
  # 🧩 Módulos locales del proyecto
  # ============================================================
  core:
    path: ../../packages/core
  auth:
    path: ../../packages/features/auth
  profile:
    path: ../../packages/features/profile
  feed:
    path: ../../packages/features/feed
  rooms:
    path: ../../packages/features/rooms
  referees:
    path: ../../packages/features/referees
  matches:
    path: ../../packages/features/matches
  tournaments:
    path: ../../packages/features/tournaments
  vip:
    path: ../../packages/features/vip
  notifications:
    path: ../../packages/features/notifications
  settings:
    path: ../../packages/features/settings

dependency_overrides:
  record_platform_interface: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/images/
    - assets/lottie/
    - assets/audio/
    - assets/audio/referee_whistle.wav
    - assets/audio/referee_whistle2.wav
    - assets/audio/referee_whistle.caf
    - assets/audio/referee_whistle2.caf
    - assets/icons/google_logo.png
    - assets/icons/facebook_logo.png
    - assets/backgrounds/login_bg.png 

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/draftclub_logo.png"
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: "assets/icons/draftclub_logo.png"
  remove_alpha_ios: true
